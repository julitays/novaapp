# Core
ENV=dev
DEBUG=true
APP_NAME=NovaProfile API
API_V1_PREFIX=/api/v1

# CORS (через запятую список доменов фронта)
# CORS_ORIGINS= [http://localhost:5173]

# DB (заменим на Supabase позже)
DB_URL=postgresql+psycopg://postgres:postgres@localhost:5432/postgres

# Auth
JWT_SECRET=dev-please-change-this-long-random-string-42
ACCESS_TTL_MIN=15
REFRESH_TTL_DAYS=7
ALGO=HS256

# Bootstrap admin (MVP)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=Admin123!

# Storage (от Supabase — добавим на Фазе 7)
STORAGE_BUCKET=avatars
STORAGE_PUBLIC_URL=
STORAGE_SERVICE_KEY=
